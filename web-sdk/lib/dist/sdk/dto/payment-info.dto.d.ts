export declare class PaymentInfoDTO {
    cardNumber: string;
    cvv: string;
    expirationDate: string;
    amount: string;
    constructor(cardNumber: string, cvv: string, expirationDate: string, amount: string);
}
