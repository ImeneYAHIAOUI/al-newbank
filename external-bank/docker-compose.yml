version: "3.3"

services:
  externalbank-service:
    image: "newbank/external-bank"
    container_name: "external-bank-service"
    restart: always
    ports:
      - 5010:8080
    networks:
      - spring-newbank-network

networks:
  spring-newbank-network:
    driver: bridge